# Server settings
UVICORN_PORT=8050
STREAMLIT_PORT=8502
REDIS_DB=5
REDIS_POST=6379

# Ngrok Configuration (requires paid plan with reserved domain)
# Set to true to automatically start ngrok with run_streamlit.sh
USE_NGROK=false
# Example: NGROK_DOMAIN=ocapistaine.ngrok-free.app
NGROK_DOMAIN=

# Streamlit CORS Configuration
# Comma-separated list of allowed origins
# Include your ngrok domain, production domain, and localhost for development
# Example: https://ocapistaine.vaettir.locki.io,https://ocapistaine.ngrok-free.app,http://localhost:8502
STREAMLIT_SERVER_ALLOWED_ORIGINS=http://localhost:8502

# Optional: Set production domain for browser config
# STREAMLIT_BROWSER_SERVER_ADDRESS=ocapistaine.vaettir.locki.io
# STREAMLIT_BROWSER_SERVER_PORT=443
# Firecrawl API Configuration
# Get your API key from: https://firecrawl.dev
REDIS_URL=redis://localhost:6379/5
FIRECRAWL_API_KEY=your_key
OPENAI_API_KEY=your_key
OPIK_API_KEY=your_key
OPIK_WORKSPACE=ocapistaine-dev

# Optional
VECTOR_STORE_TYPE=chroma  # or pinecone, qdrant
APP_ENV=development       # or production

# Optional: OCR Configuration (for future use)
# TESSERACT_PATH=/usr/local/bin/tesseract
# OCR_LANGUAGE=fra
